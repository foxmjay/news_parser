version: '2'
services:
    web:
        build: .
        ports:
                - "127.0.0.1:18080:18080"
        volumes:
            - .:/code
        depends_on:
            - redis
    redis:
        image: redis
